/**
 * Adapter for CSV round-trip. Load/save go through API; this module can hold
 * client-side validation/export helpers that use @data-core (browser-safe) if needed.
 */

import type { AuthoringModel } from '../api/authoringClient'

export type { AuthoringModel }
