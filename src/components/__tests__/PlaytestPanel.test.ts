import { describe, it, expect } from 'vitest'
import PlaytestPanel from '../PlaytestPanel.vue'

describe('PlaytestPanel', () => {
  it('exports a component', () => {
    expect(PlaytestPanel).toBeDefined()
  })
})
